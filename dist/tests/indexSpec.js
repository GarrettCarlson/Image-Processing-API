"use strict";
// middleware tests
describe('Middleware spec', function () {
    // fetched params from url
    // image source name
    // corresponding image should exist on the server
    // if the image does not exist, an error should be provided to the user
    // if the image has already been resized to the requested dimensions, we should recognize that
    // if the image fails to process, an error should be provided to the user
    // new height/width
    // should be positive integers
    // should not be too large or small (within package, then memory constraints)
});
